# Emergency Restore — Visible Images

Purpose: restore the last known-safe image architecture.

What this restores:
- Real `<img class="hero-image">` hero images.
- Visible in-page images.
- OTOS brain logo in the top-left nav.
- Normal page scrolling.
- No fixed/sticky image experiments.
- No static background-only image dependency.

Upload the contents of this folder to the `final-prepilot-partner-portal` branch root and overwrite existing files.

Commit message: `Emergency restore visible images`

After upload: wait for Vercel, then hard refresh with Cmd + Shift + R.
