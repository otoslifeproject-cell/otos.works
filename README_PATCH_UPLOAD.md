# OTOS hero/logo patch - small upload

Upload the contents of this folder into the root of the GitHub branch `final-prepilot-partner-portal`.

This patch intentionally does NOT include all site images, because your diagnostic page proved those are already uploaded and loading.

It replaces:
- all HTML pages, so they use the real logo and cache-busted CSS
- css/otos.css, so hero images are visible and not buried under a black overlay
- logo image files only, as a safety copy

Do not upload the zip itself. Upload the files and folders inside it.
