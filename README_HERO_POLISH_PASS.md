# OTOS Hero Polish Pass

Upload the contents of this folder to the `final-prepilot-partner-portal` branch.

This is not a rebuild. It keeps the current page copy and image filenames, but adds:

- smoother responsive scaling using `clamp()` rather than hard jumps
- page-specific hero classes
- per-page hero crop/focal-point rules
- lighter, more image-respecting overlays
- smaller/controlled hero headlines where they were overpowering images
- cache-busted CSS: `css/otos.css?v=hero-polish-1`

Upload contents, not the zip itself. Commit message suggestion:

`Apply hero polish responsive pass`
